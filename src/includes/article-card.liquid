<h2>
    <a
        class="u-url u-uid p-name"
        href="{{ article.url }}"
    >
        {{ article.data.title -}}
    </a>
</h2>
<p class="text-sm dt-published">{{ article.date | friendlyDate }}</p>
<article>
   {{ article.templateContent }}
</article>